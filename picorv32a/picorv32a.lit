; REQUIRES: flow-picorv32a
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/picorv32a/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/picorv32a/default/home/checklist.json
