; REQUIRES: flow-aes
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/aes/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/aes/default/home/checklist.json
