; REQUIRES: flow-y_huff
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/y_huff/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/y_huff/default/home/checklist.json
