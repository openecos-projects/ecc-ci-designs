; REQUIRES: flow-usb
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/usb/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/usb/default/home/checklist.json
