; REQUIRES: flow-BM64
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/BM64/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/BM64/default/home/checklist.json
