; REQUIRES: flow-APU
; RUN: %jq -e "[.steps[].state] | all(. == \"Success\")" %flows/APU/default/home/flow.json
; RUN: %jq -e "[.checklist[] | select(.category == \"quality_gate\")] | length > 0 and all(.state == \"pass\" and (.blocked | not))" %flows/APU/default/home/checklist.json
